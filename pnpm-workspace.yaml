packages:
  - "apps/*"
  - "packages/*"

# pnpm >=10 blocks postinstall scripts unless approved.
# isolated-vm compiles a native module; esbuild installs its platform binary.
onlyBuiltDependencies:
  - isolated-vm
  - esbuild
