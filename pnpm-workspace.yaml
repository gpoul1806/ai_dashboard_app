packages:
  - "apps/*"
  - "packages/*"

# pnpm >=10 blocks dependency build scripts unless explicitly allowed.
# esbuild installs its platform binary; isolated-vm compiles a native module
# (optional — falls back to worker_threads if the build is unavailable).
allowBuilds:
  esbuild: true
  isolated-vm: true
